{
  "name": "advanced-download-manager",
  "version": "0.0.2",
  "private": true,
  "description": "Chrome extension that intercepts downloads and routes them to local/remote download managers with cookie forwarding",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/**/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}